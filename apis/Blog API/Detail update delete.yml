info:
  name: Detail update delete
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:8000/api/posts/:slug/
  params:
    - name: slug
      value: apa-itu-pemrograman
      type: path
  body:
    type: multipart-form
    data:
      - name: status
        type: text
        value: PUBLISHED
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5