info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/register/
  body:
    type: json
    data: |-
      {
        "first_name": "Rendi",
        "last_name": "Putra",
        "username": "rendiputra1",
        "email": "rendi1@gmail.com",
        "password": "root"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
