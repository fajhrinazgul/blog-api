info:
  name: Token
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/token/
  body:
    type: json
    data: |-
      {
        "username": "rendiputra1",
        "password": "root"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
