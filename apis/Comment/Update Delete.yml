info:
  name: Update Delete
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:8000/api/posts/:slug/comments/:id/
  params:
    - name: id
      value: "21"
      type: path
    - name: slug
      value: belajar-bahasa-pemrograman-golang
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
